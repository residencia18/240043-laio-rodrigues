import { MaiorDeIdadeDirective } from './maior-de-idade.directive';

describe('MaiorDeIdadeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaiorDeIdadeDirective();
    expect(directive).toBeTruthy();
  });
});
