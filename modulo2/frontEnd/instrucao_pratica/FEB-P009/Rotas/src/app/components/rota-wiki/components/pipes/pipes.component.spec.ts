import { BoldSearchPipe } from "./pipes.component";

describe('BoldSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
